# Render blueprint: one always-on web service. It serves the public site (Plaibook Stats) and the /v1
# API, runs the crawl worker, and (SCHEDULER_ENABLED=1) enqueues the hourly / nightly / weekly jobs itself.
# COLLEGE_TRIGGER_SECRET gates only the admin console (/admin, Jobs, Quality) and the job routes.
services:
  - type: web
    name: plaibook-college-scraper
    runtime: docker
    plan: starter
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: COLLEGE_TRIGGER_SECRET
        generateValue: true
      - key: CONTACT_EMAIL
        value: hoosieraisolutions@gmail.com
      - key: COLLEGE_ALLOW_PROD
        value: "1"
      - key: SCHEDULER_ENABLED
        value: "1"
      - key: NCAA_API_BASE
        sync: false
      - key: COLLEGE_API_KEYS
        sync: false
      - key: CORS_ORIGINS
        value: https://www.plaibook.soccer,https://plaibook.soccer
      - key: API_RATE_LIMIT_PER_MIN
        value: "600"
      # The keyless free tier, per client IP.
      - key: API_ANON_RATE_LIMIT_PER_MIN
        value: "60"
      - key: PUBLIC_URL
        value: https://plaibook-college-scraper.onrender.com
      - key: LOG_LEVEL
        value: info
      - key: PORT
        value: "8080"
